# Task Artifacts — monorepo-agents-harness (root)

Per-task plan/spec/memory/verify history for the harness's own root workspace lives here:
`task_<YYYY_MM_DD>_<slug>/` directories cataloged by `index.md`. **`index.md` is the entry point** —
read it before scanning task dirs, and update it in the SAME commit as any task-dir add/update/delete.

Full indexing rules and formats: `core/governance/artifacts/AGENTS.md`

<!-- Manually seeded (this repo has no apps/packages workspaces for scaffold-workspace-agents.sh to
     target) — keep this pointer file in sync with core/governance/artifacts/workspace-AGENTS.md. -->
