# install-manifest.txt — the exact whitelist of what gets installed into
# <repo-root>/.agents/monorepo-agents-harness/.
#
# One source-relative path per line (file or directory). Anything NOT listed here is never
# installed. Read by core/scripts/install-harness.sh (executes it) and
# core/scripts/audit-install.sh (verifies against it) — they must never keep separate lists.
#
# Blank lines and lines starting with # are ignored.
#
# Deliberately absent, do not add:
#   AGENTS.md, adapters/AGENTS.md   guidelines for developing THIS template repo — meaningless in
#                                   a consumer project (the consumer's own root AGENTS.md is
#                                   generated from core/root-AGENTS.md instead)
#   changelogs/                     upgrade prompts are always read from a fresh temporary clone,
#                                   never from the installed copy
#   .gitignore, .github/            this repo's own tooling

core
# Starter project-rule template for the consumer's .agents/rules/ — copied into the bundle here,
# then seeded into <repo-root>/.agents/rules/ by core/scripts/scaffold-project-agents.sh (never
# overwrites an existing rule). Listed explicitly, not just via the `core` directory row above, so
# the consumer-facing seed artifact is a first-class, audited install row.
core/project-rules-template/local-agents-md.md
# Repo-root knowledge base seed (Karpathy "LLM Wiki": compiled layer agents query first) — copied
# into the bundle here, then seeded into <repo-root>/knowledge/ by core/scripts/scaffold-knowledge.sh
# (never overwrites consumer content). Listed explicitly so the seed is a first-class audited row.
core/knowledge-template
# The knowledge-base skill is agent-neutral and referenced from core/root-AGENTS.md — shipped via
# the `core` row above through the manifest, and executed by the knowledge base's own seed/ingest
# scripts rather than a per-agent copy.
core/skills/knowledge-base/SKILL.md
adapters/claude-code
adapters/codex
adapters/opencode
VERSION
README.md
INSTALL.md
PORTABILITY.md
CHANGELOG.md
LICENSE
