# Task Artifacts — <workspace>

Per-task plan/spec/memory history for this workspace lives here: `task_<YYYY_MM_DD>_<slug>/`
directories cataloged by `index.md`. **`index.md` is the entry point** — read it before scanning
task dirs, and update it in the SAME commit as any task-dir add/update/delete.

Full indexing rules and formats:
`.agents/monorepo-agents-harness/core/governance/artifacts/AGENTS.md`

<!-- Seeded by .agents/monorepo-agents-harness/core/scripts/scaffold-workspace-agents.sh — keep this pointer file. -->
