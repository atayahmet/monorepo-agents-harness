# Intent Inbox — <workspace>

Stakeholder problem descriptions, captured before they're scoped into a plan-mode task, live here:
`intent_<YYYY_MM_DD>_<slug>.md` files, one per intent. `status: pending | approved | rejected` in
each file's frontmatter tracks its review state — files are never deleted, regardless of decision.

Full format and lifecycle rules:
`.agents/monorepo-agents-harness/core/governance/intents/AGENTS.md`

<!-- Seeded by .agents/monorepo-agents-harness/core/scripts/scaffold-workspace-agents.sh — keep this pointer file. -->
