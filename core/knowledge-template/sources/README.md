# Sources

One `[task_slug].md` synopsis page per ingested task: one-paragraph summary plus a pointer to the
raw task directory. Compiled by the ingest workflow (`core/scripts/kb-ingest.sh`). These are
pointers — the raw task artifacts under `.agents/artifacts/` stay the immutable source of truth. See
`../schema.md` for citation rules.

<!-- Index rows for sources live in the catalog of ../index.md. -->