# Concepts

One `[snake_case].md` page per recurring pattern, approach, or durable synthesis extracted from task
memory. Compiled by the ingest workflow (`core/scripts/kb-ingest.sh`) and strengthened or challenged
across tasks. See `../schema.md` for naming, link, and citation rules.

<!-- Index rows for concepts live in the catalog of ../index.md. -->