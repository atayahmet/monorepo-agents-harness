# Modules

One `[snake_case].md` page per workspace or package that accumulated durable knowledge. Compiled
from `3_memory.md` outcomes by the ingest workflow (`core/scripts/kb-ingest.sh`). See `../schema.md`
for naming, link, and citation rules.

<!-- Index rows for modules live in the catalog of ../index.md. -->