# Project Overview

High-level, co-evolved picture of this repository: architecture, module topology, and the decisions
that shape them. Written by agents during `kb-ingest` when a task's memory changes the overview, and
reviewed during the lint workflow.

## What this project is

<!-- One paragraph: the project's purpose, its monorepo layout (apps/packages), and the primary
     workspace(s). Keep current; this is the page a new agent reads first for orientation. -->

## Module topology

<!-- Bulleted map of workspaces/modules and their relationships. Update when a module is added,
     renamed, or removed. Cross-link each row to its `modules/<name>.md` page when one exists. -->

## Guiding decisions

<!-- One line per architecture-affecting decision, linking the `decision-records/NNNN-<title>.md`
     page. Kept in sync with the ADR index row by the ingest workflow. -->