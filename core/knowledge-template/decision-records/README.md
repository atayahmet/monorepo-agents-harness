# Decision Records

Compiled copies of the architecture decision records each task produced (`adr/NNNN-<title>.md`),
kept under `NNNN-<title>.md` so numbering round-trips. Compiled by the ingest workflow
(`core/scripts/kb-ingest.sh`); the raw ADR in the task directory stays the source of truth. See
`../schema.md` for citation rules.

<!-- Index rows for decisions live in the catalog of ../index.md. -->